meta {
  name: Reserve Stock
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/inventory/reserve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "itemId": 1,
    "quantity": 50
  }
}
