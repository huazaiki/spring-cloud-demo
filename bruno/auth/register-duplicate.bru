meta {
  name: 用户注册—用户名已存在 (400)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "pass123",
    "role": "ADMIN"
  }
}

headers {
  Content-Type: application/json
}

script:post-response {
  bru.test("Status is 400", () => {
    expect(res.status).to.equal(400);
  });
}
