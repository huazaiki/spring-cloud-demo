meta {
  name: 用户注册
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "pass123",
    "role": "ADMIN"
  }
}

headers {
  Content-Type: application/json
}

script:post-response {
  bru.test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });

  bru.test("Response code is 200", () => {
    expect(res.data.code).to.equal(200);
  });
}
