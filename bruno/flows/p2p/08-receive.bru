meta {
  name: 08-入库
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/inventory/receive
  body: json
  auth: inherit
}

body:json {
  {
    "orderId": {{orderId}},
    "itemId": {{itemId}},
    "quantity": 10
  }
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

script:post-response {
  bru.test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });
}
