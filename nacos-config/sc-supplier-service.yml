# Nacos dataId: supplier-service.yml   group: DEFAULT_GROUP
spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration/supplier-service
  datasource:
    url: jdbc:mysql://localhost:3306/supplier_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai