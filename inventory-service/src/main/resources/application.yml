server:
  port: 8084

spring:
  application:
    name: inventory-service
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        group: DEFAULT_GROUP
        file-extension: yml
  config:
    import:
      - optional:nacos:common.yml?group=DEFAULT_GROUP&refreshEnabled=true
      - optional:nacos:inventory-service.yml?group=DEFAULT_GROUP&refreshEnabled=true
