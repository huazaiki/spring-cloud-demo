## Ubiquitous Language

| Term | Definition |
|---|---|
| **Supplier** | 供应商——提供商品或服务的外包组织。核心属性：名称、统一社会信用代码、联系人、资质状态。 |
| **Item** | 物料——可被采购、入库、库存管理的商品或原材料。核心属性：名称、规格、计量单位、SKU。 |
| **Purchase Order** | 采购订单——采购方发给供应商的正式采购凭证，包含物料明细、数量、单价、金额。有状态流转：草稿 → 已审批 → 已发货 → 已入库 → 已结清。 |
| **Receive** | 入库——物料实物到达仓库后的验收确认操作。每次入库关联一个采购订单，可分批入库。 |
| **Inventory** | 库存——某个物料在仓库中的在库数量，含可用库存和已预留库存。 |
| **Stock Reservation** | 库存预留——采购订单审批通过后，预先锁定对应数量的库存，防止被其他订单占用。 |
| **Payment** | 应付账款——入库完成后生成的供应商应付款项，含金额、账期、付款状态。 |
| **Gateway** | API 网关——所有外部请求的统一入口，负责路由转发、token 校验、限流。 |
| **TraceId** | 链路追踪 ID——贯穿整个请求链路的唯一标识，由 Micrometer Tracing 自动生成和传播。 |

| **Purchase Requisition** | 请购（PR）——采购需求的正式申请单据，审批通过后汇总或转为采购订单。 |
| **Approval Flow** | 审批链——可配置的审批流程定义，决定单据的审批节点、条件与审批人。 |
| **Approval Task** | 审批任务——审批链上的具体待办任务实例，由审批人处理（通过/驳回/转交）。 |
| **Role** | 角色——一组权限点的集合，分配给用户；用户可拥有多个角色。 |
| **Permission** | 权限点——可授权的细粒度操作标识（如 purchase:order:approve），角色与权限点多对多关联。 |
| **Department** | 部门——组织的树形结构单元，是数据权限的边界之一。 |
| **Data Scope** | 数据权限——用户可访问数据的范围约束（如仅本部门、本部门及下级）。 |
| **Supplier Qualification** | 供应商准入——供应商进入合格供方目录前的资质审核流程。 |
| **Quality Inspection** | 质检——入库前的质量检验环节（免检/全检/抽检），不合格品不得入库。 |
| **Three-Way Match** | 三单匹配——采购订单、收货单、发票三者数量与金额的核对过程。 |