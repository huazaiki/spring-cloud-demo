## Ubiquitous Language

| Term | Definition |
|---|---|
| **Supplier** | 供应商——提供商品或服务的外包组织。核心属性：名称、统一社会信用代码、联系人、资质状态。 |
| **Item** | 物料——可被采购、入库、库存管理的商品或原材料。核心属性：名称、规格、计量单位、SKU。 |
| **Purchase Requisition** | 请购（PR）——采购需求的正式申请单据，审批通过后汇总或转为采购订单。状态：草稿 → 已提交 → 审批中 → 已通过/已驳回。 |
| **Purchase Order** | 采购订单——采购方发给供应商的正式采购凭证，包含物料明细、数量、单价、金额。状态：草稿 → 已确认 → 已发货 → 已入库 → 已结清。 |
| **Receive** | 入库——物料实物到达仓库后的验收确认操作。每次入库关联一个采购订单，可分批入库。 |
| **Inventory** | 库存——某个物料在仓库中的在库数量，含可用库存和已预留库存。 |
| **Stock Reservation** | 库存预留——采购订单审批通过后，预先锁定对应数量的库存，防止被其他订单占用。 |
| **Payment** | 应付账款——入库完成后生成的供应商应付款项，含金额、账期、付款状态。 |
| **Gateway** | API 网关——所有外部请求的统一入口，负责路由转发、token 校验、限流。 |
| **TraceId** | 链路追踪 ID——贯穿整个请求链路的唯一标识，由 Micrometer Tracing 自动生成和传播。 |
| **Role** | 角色——一组权限点的集合，分配给用户；用户可拥有多个角色。系统预置 6 个标准角色：系统管理员、需求申请人、部门审批人、采购员、仓储管理员、财务专员。 |
| **System Admin** | 系统管理员——负责用户、角色、权限、部门及基础数据维护的超级角色，拥有全部系统管理权限。 |
| **Requester** | 需求申请人——负责提交采购申请、跟踪申请进度的角色。仅可见自己创建的请购单。 |
| **Department Approver** | 部门审批人——负责审批本部门采购申请的角色。审批引擎查询其所属部门内所有该角色成员生成待办任务池。 |
| **Buyer** | 采购员——负责供应商管理、物料管理、询价、创建和跟踪采购订单的角色。按供应商或品类划分数据可见范围。 |
| **Warehouse Admin** | 仓储管理员——负责收货确认、入库操作、库存管理与调整的角色，全局可见库存数据。 |
| **Finance Specialist** | 财务专员——负责发票审核、付款审批、付款管理及应付账款查询的角色，全局可见财务数据。 |
| **Permission** | 权限点——可授权的细粒度操作标识，命名规范为 `domain:resource:action`（如 `purchase:order:approve`），角色与权限点多对多关联。 |
| **Department** | 部门——组织的树形结构单元，是审批路由和数据权限的边界之一。 |
| **Data Scope** | 数据权限——用户可访问数据的范围约束。各角色边界：需求申请人仅自己的请购单；部门审批人仅本部门的请购单；采购员按供应商/品类划分；仓储管理员和财务专员为全局可见。 |
| **Approval Flow** | 审批链——可配置的审批流程定义，决定单据的审批节点、条件与审批人。节点支持按角色（如 ROLE_DEPT_APPROVER）在部门范围内动态查找审批人，生成待办池。 |
| **Approval Task** | 审批任务——审批链上的具体待办任务实例，由审批人处理（通过/驳回/转交）。驳回后单据退回草稿状态，申请人可修改后重新提交。 |
| **Supplier Qualification** | 供应商准入——供应商进入合格供方目录前的资质审核流程。 |
| **Quality Inspection** | 质检——入库前的质量检验环节（免检/全检/抽检），不合格品不得入库。 |
| **Three-Way Match** | 三单匹配——采购订单、收货单、发票三者数量与金额的核对过程。 |
| **Receive Order** | 收货单——按采购订单收货的凭证，支持分批收货与多收/少收差异登记，明细逐行与订单核对。 |
| **Inventory Ledger** | 库存流水——库存每次变动（预留/释放/入库/出库/盘点）的逐笔记录，用于追溯与对账。 |
| **Invoice** | 发票——供应商开具的进项发票，登记后参与三单匹配与付款。 |
| **Payment Voucher** | 付款单——对供应商付款的凭证，经审批后执行，付款后核销应付账款。 |
| **Settlement** | 核销——将付款单与应付账款关联冲抵，确保账实相符。 |
