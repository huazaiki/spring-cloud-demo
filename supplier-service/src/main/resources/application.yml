server:
  port: 8082

spring:
  application:
    name: supplier-service
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        group: DEFAULT_GROUP
        file-extension: yml
  config:
    import:
      - optional:nacos:common.yml?group=DEFAULT_GROUP&refreshEnabled=true
      - optional:nacos:supplier-service.yml?group=DEFAULT_GROUP&refreshEnabled=true
